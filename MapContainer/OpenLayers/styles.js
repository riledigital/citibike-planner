import styled from "styled-components";

export const StyledMapElement = styled.div`
  display: block;
  position: fixed;
  top: var(--h-header);
  right: 0;
  bottom: 0;
  left: 0;
  height: 100%;
  width: 100%;
`;
