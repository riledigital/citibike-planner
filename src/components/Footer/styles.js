import styled from "styled-components";

export const StyledFooter = styled.footer`
  .App-footer {
    grid-column: 1 / 10;
    /* grid-row: 3; */
    /* padding: 2rem; */
  }

  .App-footer {
    align-self: end;
    color: var(--c-text);
    /* padding: 1rem; */
  }
`;
