import React, { useState, useEffect } from "react";

function Map(props) {
  const { handleMapMouseover, handleStationClick } = props;
  // handleMapMouseover.bind(this);
  // const [state, setState] = useState({
  //   lng: coords.lon,
  //   lat: coords.lat,
  //   zoom: 11,
  // });
}

export default Map;
