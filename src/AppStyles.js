import styled from "styled-components";

export const StyledMap = styled.div`
  /* Fit entire map to screen */
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  z-index: 0;
  min-height: 90vh;
  min-width: 90vw;
`;
